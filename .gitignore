# dependencies / build output
web/node_modules
web/dist

# env files (never commit your API URL secret-ish config)
web/.env
web/.env.local

# local machine settings (may contain tokens/paths — never publish)
.claude/settings.local.json

# OS / editor
.DS_Store
*.log
